import { MockConfig } from './interfaces';

/**
 * Helpers function for define mock config
 * @param config
 * @returns
 */
export function defineMockConfig(config: MockConfig[] = []) {
    return config;
}
